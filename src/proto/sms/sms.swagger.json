{
  "swagger": "2.0",
  "info": {
    "title": "proto/sms/sms.proto",
    "version": "version not set"
  },
  "tags": [
    {
      "name": "SMS"
    }
  ],
  "consumes": [
    "application/json"
  ],
  "produces": [
    "application/json"
  ],
  "paths": {
    "/sms.SMS/Send": {
      "post": {
        "operationId": "SMS_Send",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "properties": {}
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/smsSendRequest"
            }
          }
        ],
        "tags": [
          "SMS"
        ]
      }
    },
    "/sms.SMS/Validation": {
      "post": {
        "operationId": "SMS_Validation",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "properties": {}
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/smsValidationRequest"
            }
          }
        ],
        "tags": [
          "SMS"
        ]
      }
    }
  },
  "definitions": {
    "protobufAny": {
      "type": "object",
      "properties": {
        "@type": {
          "type": "string"
        }
      },
      "additionalProperties": {}
    },
    "rpcStatus": {
      "type": "object",
      "properties": {
        "code": {
          "type": "integer",
          "format": "int32"
        },
        "message": {
          "type": "string"
        },
        "details": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/protobufAny"
          }
        }
      }
    },
    "smsSendRequest": {
      "type": "object",
      "properties": {
        "mobile": {
          "type": "string"
        }
      }
    },
    "smsValidationRequest": {
      "type": "object",
      "properties": {
        "mobile": {
          "type": "string"
        },
        "code": {
          "type": "string"
        }
      }
    }
  }
}
